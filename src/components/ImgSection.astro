---
import { ContainerImage } from "./react/ContainerImage"

const {bgColor} = Astro.props
---

<div class=`w-[500px] h-[270px] bg-[${bgColor || 'var(--second-bg)'}] rounded-3xl`>
    <ContainerImage />
</div>